---
task_id: T08
title: Architecture and scope invariant proof
role: deepseek-builder

objective: >
  Provide mechanical, repository-native proof of the accepted architecture and scope
  invariants: extend the boundary checks to cover every production frontend root
  (src/app, src/api, src/features) with allow and forbid cases, prove URL-owned list
  operations with no client-side re-filtering or re-sorting, no action update/delete,
  no second server-state store, and non-color-only aging communication, and prove that
  the design-only production backend and infrastructure were not implemented.

base_commit: 517a0218e64f7d7de5722310e1808701cff32150
worktree: /Users/tunguyen/Projects/keyloop-challenge/.worktrees/t08-arch-proof (branch task/t08-arch-proof)

design_refs:
  - docs/system-design/system-design-v1.md#3.4-module-boundaries
  - docs/system-design/system-design-v1.md#6.1-state-ownership
  - docs/system-design/system-design-v1.md#6.8-server-side-list-operations
  - docs/system-design/system-design-v1.md#6.11-mock-backend
  - docs/system-design/system-design-v1.md#7.2-production-backend-design
  - docs/agents/README.md#project-specific-non-negotiables
  - docs/agents/QUALITY_AND_LEARNING.md#gate-e--architecture-compliance-proof
  - docs/decisions/0001-mock-implementation-baseline.md#decision

requirements:
  - ARCH-SCOPE-001
  - TEST-005

test_seams:
  - Boundary seam: mechanical checks reject production imports of mock repositories or browser storage and reject client recomputation of inventoryAgeDays or isAging across ALL production roots (src/app, src/api, src/features), and accept compliant code, with at least one negative fixture and one positive case.
  - Scope seam: an independent mechanical check proves no production backend or infrastructure was implemented (no Fastify, Prisma, PostgreSQL, sync worker, IdP, or observability backend) with allowed and forbidden cases.
  - List-operations seam: mechanical checks prove filters, sort, and page are URL-owned, that no client re-filtering or re-sorting of a returned page or second server-state store exists, and that no action update/delete surface exists.
  - Aging-communication seam: a mechanical check proves aging status is not communicated by color alone.
  - Carried T04/T05 finding: the checks must cover src/features/** so a future violation there is rejected mechanically.

dependencies:
  - T06 DONE and integrated at eb1056ec69b4632da78dae3487724e5dcaa21dd0
  - Existing src/test/architecture.test.ts checks, currently scoped to src/app and src/api only

allowed_scope:
  - src/test/architecture.test.ts and additional src/test/*.test.ts check files
  - test-only helper modules under src/test
  - minimal configuration changes required to run the checks
  - NO production source changes: if a check reveals a genuine production violation, STOP and report it instead of editing product code

out_of_scope:
  - production source changes
  - browser/e2e proof (T07)
  - changing mock/API behavior, semantics, fixtures, endpoints, or error contracts
  - modifying System Design, decisions, Harness/core, plans, task contracts, or evidence
  - unrelated cleanup

acceptance:
  - Mechanical checks reject: production import of mock internals or browser storage; client recomputation of inventoryAgeDays or isAging; a second server-state store; client re-filtering or re-sorting of returned pages; any action update/delete surface; color-only aging. Each with a negative fixture.
  - The same checks accept compliant production code with no false positives.
  - An independent check proves the design-only production backend and infrastructure were not implemented, with allowed and forbidden cases.
  - The checks cover src/app, src/api, AND src/features.
  - TEST-005 is satisfied and the previously carried STD-01/F3 coverage gap is closed.
  - No production source change is made.

required_validation:
  - npm ci
  - npm run typecheck
  - npm run lint
  - npm run test
  - npm run build
  - Run the architecture checks and report the named check results, including the negative fixtures.
  - git diff --check 517a0218e64f7d7de5722310e1808701cff32150...<task_commit>
  - Confirm the diff stays within T08 scope

forbidden:
  - modify production source code
  - change System Design, accepted decisions, or product semantics
  - weaken or delete existing architecture assertions
  - implement T07 browser proof
  - modify durable workflow artifacts from the Builder branch
  - perform unrelated cleanup

output:
  - coherent task commit or small coherent commit series based on 517a0218e64f7d7de5722310e1808701cff32150
  - concise evidence capsule using the repository template
  - requirement-to-implementation mapping
  - exact changed paths and command results
  - red-green evidence for each public seam
  - scope expansion and design deviations, which must be empty unless escalated
  - unresolved risks or blockers
---
