---
task_id: T05
title: Responsive vehicle detail, action form, and action history with optimistic creation
role: deepseek-builder

objective: >
  Implement the responsive vehicle detail surface on top of the T04 dashboard and the
  T03 HTTP/mock backend: a detail surface presenting vehicle summary, current action,
  a create-action form populated from dynamic active statuses, and complete action
  history newest-first; plus optimistic action creation that reconciles with the
  authoritative response and rolls back on failure while keeping the inventory list,
  detail, and history coherent.

base_commit: 38ff2a9641e8ead7acf96aad8d1d1fff864a825e
worktree: /Users/tunguyen/Projects/keyloop-challenge/.worktrees/t05-action-ui (branch task/t05-action-ui)

design_refs:
  - docs/system-design/system-design-v1.md#4.5-vehicle-actions
  - docs/system-design/system-design-v1.md#4.6-frontend-facing-vehicle-view
  - docs/system-design/system-design-v1.md#5.1-core-rest-endpoints
  - docs/system-design/system-design-v1.md#5.5-action-statuses
  - docs/system-design/system-design-v1.md#5.6-action-history
  - docs/system-design/system-design-v1.md#5.7-creating-an-action
  - docs/system-design/system-design-v1.md#6.1-state-ownership
  - docs/system-design/system-design-v1.md#6.5-vehicle-detail
  - docs/system-design/system-design-v1.md#6.6-action-history
  - docs/system-design/system-design-v1.md#6.7-optimistic-action-creation
  - docs/system-design/system-design-v1.md#6.10-accessibility
  - docs/decisions/0001-mock-implementation-baseline.md#decision

requirements:
  - DETAIL-001
  - RESP-003
  - DETAIL-002
  - ACT-008
  - ACT-009

rules_to_load:
  - AGENTS.md#keyloop-challenge-autonomous-workflow
  - docs/agents/README.md#project-specific-non-negotiables
  - docs/agents/QUALITY_AND_LEARNING.md#gate-c--frontend-integration-proof
  - docs/agents/QUALITY_AND_LEARNING.md#autonomous-tdd-compatibility
  - .agents/skills/tdd/SKILL.md
  - .agents/skills/tdd/tests.md
  - .agents/skills/tdd/mocking.md

test_seams:
  - Detail surface seam: selecting a vehicle opens a detail surface that presents, in order, the vehicle summary, the current action, the create-action form, and the complete action history.
  - Responsive detail seam: desktop uses a right-side drawer, tablet an adaptive wide sheet, and mobile a full-screen detail surface.
  - Action-history seam: the detail loads GET /vehicles/:vehicleId/actions and renders the complete immutable history newest-first with status label, actor display name, timestamp, and note (note may be absent or null).
  - Dynamic-status form seam: the create-action form populates its status options from GET /vehicle-action-statuses (active statuses only) and submits only statusId plus the optional note.
  - Optimistic mutation seam: submitting the form optimistically sets the current action in both the list and the detail, then POSTs; on success it reconciles with the authoritative created action; on failure it rolls back to the previous current action and surfaces the failure regionally.
  - Cross-view coherence seam: after a successful create, the inventory list row current action, the detail current action, and the detail history all reflect the new action consistently through TanStack Query.

dependencies:
  - T04 DONE and integrated at 4c28564e8cccc1ee69cfa402d87140cd7a1f12e7
  - T03 DONE HTTP/mock endpoints: GET /vehicle-action-statuses, GET /vehicles/:vehicleId/actions, POST /vehicles/:vehicleId/actions, and GET /vehicles current-action composition
  - Typed ApiClient functions getVehicleActionStatuses, getVehicleActions, createVehicleAction, plus the T04 dashboard/feature shell and router

allowed_scope:
  - vehicle detail surface (drawer/sheet/fullscreen) and its styles, under src/features/inventory or a new src/features/actions directory wired through the existing router/feature shell
  - a route or search-param mechanism to open and close the detail surface that preserves the T04 URL discovery state
  - TanStack Query hooks for action history and active statuses, and a create-action mutation built on the existing ApiClient
  - create-action form (dynamic status select, optional note) and the action-history list component
  - current-action display in the inventory list row and in the detail surface
  - optimistic update plus rollback wiring for the current action across the affected query caches
  - focused RTL + MSW tests for the T05 requirements
  - minimal existing configuration changes strictly required to wire the detail surface

out_of_scope:
  - cross-feature error-region policy, freshness/stale warning, loading-skeleton rollout, and instrumentation (T06)
  - changing mock/API behavior, semantics, fixtures, endpoints, or error contracts (consume them as-is; escalate if a genuine gap exists)
  - production backend, infrastructure, auth, sync, or design-only architecture
  - modifying System Design, decisions, Harness/core, plans, task contracts, or evidence
  - unrelated cleanup

acceptance:
  - Selecting a vehicle opens a detail surface presenting vehicle summary, current action, create-action form, then action history, in that order.
  - Responsive: desktop right-side drawer, tablet wide sheet, mobile full-screen detail.
  - The inventory list shows each vehicle current action; the detail shows complete history newest-first with status label, actor display name, timestamp, and note.
  - The create-action form status options come from GET /vehicle-action-statuses (active only).
  - Submitting sets an optimistic current action, then reconciles with the authoritative POST response.
  - On POST failure the previous current action is restored and the failure is surfaced (regional containment is finalized by T06).
  - After success, list current action, detail current action, and detail history are coherent.
  - The note is optional (omitted, null, empty string, or any string), matching the accepted API shape.
  - Detail controls are labelled and the surface is keyboard-reachable (deeper accessibility proof is T07).
  - No T06 behavior, no mock/API semantic change, no production or infrastructure code.

required_validation:
  - npm ci
  - npm run typecheck
  - npm run lint
  - npm run test
  - npm run build
  - Focused RTL + MSW tests demonstrating each declared seam: detail render and content order; responsive tier selection; history newest-first with status/actor/time/note; dynamic active statuses; optimistic set plus authoritative reconcile; rollback on failure; cross-view coherence after success; optional note.
  - git diff --check 38ff2a9641e8ead7acf96aad8d1d1fff864a825e...<task_commit>
  - Confirm exactly one committed lockfile remains and the full diff stays within T05 scope.

forbidden:
  - change System Design, accepted decisions, or product semantics
  - change mock/API behavior, fixtures, endpoints, or error contracts
  - implement action update or delete
  - duplicate server data into a second application store instead of TanStack Query
  - recompute inventoryAgeDays/isAging in the client
  - implement T06 error, freshness, loading, or instrumentation behavior
  - introduce production backend, infrastructure, auth, or sync code
  - weaken or tautologically couple tests
  - modify durable workflow artifacts from the Builder branch
  - perform unrelated cleanup

output:
  - coherent task commit or small coherent commit series based on 38ff2a9641e8ead7acf96aad8d1d1fff864a825e
  - concise evidence capsule using the repository template
  - requirement-to-implementation mapping
  - exact changed paths and command results
  - red-green evidence for each public seam
  - scope expansion and design deviations, which must be empty unless escalated
  - unresolved risks or blockers
---
