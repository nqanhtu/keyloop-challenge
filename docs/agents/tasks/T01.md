---
task_id: T01
title: Runnable typed HTTP frontend foundation
role: antigravity-builder

objective: >
  Create the smallest runnable frontend foundation that establishes the selected
  React and TypeScript stack, typed HTTP boundary, MSW integration seam,
  TanStack Router and Query ownership, and repository-native validation commands
  without implementing inventory or action business behavior.

base_commit: c166671e61df0d36617090f226a4ffaf79960c23
worktree: /Users/tunguyen/Projects/keyloop-challenge/.worktrees/t01-foundation (branch task/t01-foundation)

design_refs:
  - docs/system-design/system-design-v1.md#5.1-core-rest-endpoints
  - docs/system-design/system-design-v1.md#5.8-error-contract
  - docs/system-design/system-design-v1.md#6.1-state-ownership
  - docs/system-design/system-design-v1.md#6.11-mock-backend
  - docs/system-design/system-design-v1.md#7.1-frontend
  - docs/agents/QUALITY_AND_LEARNING.md#gate-a-static-repository-proof
  - docs/agents/QUALITY_AND_LEARNING.md#gate-e-architecture-compliance-proof

requirements:
  - ARCH-HTTP-001
  - ARCH-STATE-001
  - TECH-001
  - ERR-001
  - TEST-001

rules_to_load:
  - AGENTS.md#keyloop-challenge-autonomous-workflow
  - docs/agents/QUALITY_AND_LEARNING.md#definition-of-done--task
  - docs/agents/QUALITY_AND_LEARNING.md#gate-a--static-repository-proof
  - docs/agents/QUALITY_AND_LEARNING.md#autonomous-tdd-compatibility
  - docs/agents/BRANCHING.md#dispatch-sequence

test_seams:
  - Application bootstrap seam: render or start the application through its real TanStack Router and TanStack Query provider composition without runtime failure.
  - HTTP client seam: issue an API request through the shared typed HTTP abstraction and preserve success plus stable ApiError code, message, requestId, and optional details without message parsing.
  - MSW boundary seam: development and test mocking intercept HTTP requests while application and feature-facing code have no direct mock-repository or browser-storage API.
  - Server-state ownership seam: fetched server data is represented through TanStack Query and is not duplicated into an independent application-level server-state store.
  - Repository command seam: a clean install from the single committed lockfile exposes deterministic typecheck, lint, test, and build commands.

dependencies:
  - none; branch and worktree must start exactly from base_commit c166671e61df0d36617090f226a4ffaf79960c23, not from the later Workflow Scribe commit

allowed_scope:
  - root package manifest and exactly one committed package-manager lockfile
  - TypeScript, build, lint, unit/integration test configuration
  - minimal application entry and bootstrap files
  - TanStack Router and TanStack Query provider/bootstrap wiring
  - typed HTTP client and API contract primitives, including stable ApiError shape
  - MSW browser/test bootstrap and handler-composition seam without inventory or action behavior
  - Vitest and React Testing Library foundation
  - Playwright configuration/bootstrap only if needed to establish the committed test contract
  - minimal placeholder route/screen required to prove application startup
  - behavior-oriented test helpers for the declared public seams
  - minimal ignore/config documentation strictly required by generated build and test tooling

out_of_scope:
  - inventory fixtures, domain filtering, sorting, pagination, or aging behavior
  - action statuses, history, persistence, eligibility, authorization, or mutations
  - dashboard KPIs, inventory table/cards, filters, detail UX, or action forms
  - production Fastify, Prisma, PostgreSQL, synchronization, IdP, or observability infrastructure
  - broad design-system work or speculative abstractions unrelated to the foundation
  - Harness-managed/core files, System Design, accepted decisions, workflow plans, contracts, or evidence

acceptance:
  - A minimal React and TypeScript application starts and builds successfully.
  - TanStack Router and TanStack Query are wired at the application boundary.
  - React, TypeScript, TanStack Router, TanStack Query, TanStack Table, MSW, Vitest, React Testing Library, and Playwright dependencies or configuration seams selected by System Design are established without later-task business behavior.
  - A shared typed HTTP abstraction exists and no consumer imports a mock repository or browser-storage layer.
  - ApiError preserves stable code, message, requestId, and optional details semantics.
  - MSW is wired at the HTTP boundary for development and tests with an explicit handler-composition seam for T02 and T03.
  - Vitest and React Testing Library run a minimal behavior-oriented test through the real provider/bootstrap seam.
  - Exactly one committed lockfile exists and package scripts expose deterministic typecheck, lint, test, and build equivalents.
  - The implementation contains no inventory or action business behavior and no production backend or infrastructure.
  - One coherent implementation commit and a concise evidence capsule report exact changed paths and validation.

required_validation:
  - Perform a clean/frozen install using the chosen committed lockfile and report the exact command.
  - Run the repository script named typecheck.
  - Run the repository script named lint.
  - Run the repository script named test in non-watch mode.
  - Run the repository script named build.
  - Prove one positive HTTP request intercepted by MSW through the shared HTTP client.
  - Prove one non-2xx response retains stable ApiError.code handling without parsing message text.
  - Inspect imports to prove app-facing code crosses HTTP and does not access mock storage/repositories directly.
  - Verify git diff c166671e61df0d36617090f226a4ffaf79960c23...<task_commit> stays within T01 scope.

forbidden:
  - change System Design, accepted decisions, or product semantics
  - modify Harness-managed/core or durable workflow files
  - implement T02 or later business behavior early
  - add direct React-feature access to browser storage or a mock repository
  - add a second server-state store alongside TanStack Query
  - introduce production backend or infrastructure code
  - weaken validation to make the scaffold pass
  - perform unrelated cleanup
  - base the Builder branch on the Workflow Scribe commit

output:
  - one coherent implementation commit based on c166671e61df0d36617090f226a4ffaf79960c23
  - concise evidence capsule using the repository template
  - exact changed paths
  - exact install, typecheck, lint, test, and build results
  - requirement-to-implementation mapping
  - scope expansion and design deviations, which must be empty unless escalated
  - unresolved risks or blockers
---
