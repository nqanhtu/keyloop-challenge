---
task_id: T02
title: Authoritative mock inventory reads and aging
role: antigravity-builder

objective: >
  Implement the inventory read model and dealership-timezone-aware aging policy
  behind the HTTP/MSW boundary, including deterministic fixtures, all documented
  list filters, default age sorting, pagination, detail, filter options, and
  freshness metadata, without implementing manager-action persistence or UI.

base_commit: 9cc7c62199a4b0ecb8d6ea51721200814e163035
worktree: /Users/tunguyen/Projects/keyloop-challenge/.worktrees/t02-inventory-http (branch task/t02-inventory-http)

design_refs:
  - docs/system-design/system-design-v1.md#1.3-key-assumptions
  - docs/system-design/system-design-v1.md#3.4-module-boundaries
  - docs/system-design/system-design-v1.md#4-data-ownership--domain-model
  - docs/system-design/system-design-v1.md#5.2-vehicle-list
  - docs/system-design/system-design-v1.md#5.4-filter-options
  - docs/system-design/system-design-v1.md#5.10-dashboard-read-flow
  - docs/system-design/system-design-v1.md#6.8-server-side-list-operations
  - docs/system-design/system-design-v1.md#6.11-mock-backend
  - docs/system-design/system-design-v1.md#9.1-unit-tests
  - docs/decisions/0001-mock-implementation-baseline.md#decision

requirements:
  - ARCH-DOM-001
  - AGE-001
  - AGE-002
  - INV-001
  - INV-002
  - INV-003
  - INV-004
  - INV-006
  - INV-007
  - TEST-002

rules_to_load:
  - AGENTS.md#keyloop-challenge-autonomous-workflow
  - docs/agents/QUALITY_AND_LEARNING.md#definition-of-done--task
  - docs/agents/QUALITY_AND_LEARNING.md#gate-b--business-rule-unit-proof
  - docs/agents/QUALITY_AND_LEARNING.md#gate-e--architecture-compliance-proof
  - docs/agents/QUALITY_AND_LEARNING.md#autonomous-tdd-compatibility
  - .agents/skills/tdd/SKILL.md
  - .agents/skills/tdd/tests.md
  - .agents/skills/tdd/mocking.md

test_seams:
  - Aging Policy seam: a caller supplies stockedAt, current instant, and dealership IANA timezone and receives literal calendar-day inventoryAgeDays plus strict isAging greater-than-90 classification.
  - Vehicle list HTTP seam: GET /vehicles accepts documented query parameters and returns VehicleListResponse after current-presence selection, filtering, sorting, and pagination behind MSW.
  - Vehicle detail HTTP seam: GET /vehicles/:vehicleId returns the designed VehicleView for a present deterministic fixture identified by stable vehicleId.
  - Filter-options HTTP seam: GET /inventory/filter-options and optional make query return dynamic make/model options derived behind the HTTP boundary.
  - Inventory composition seam: the inventory query module depends on explicit projection and current-action read interfaces; T02 uses an empty current-action reader so VehicleView.currentAction is null and actionStatusId produces no matches without implementing T03.
  - Architecture seam: projection fixtures do not persist inventoryAgeDays or isAging, and frontend app code consumes typed inventory API functions rather than mock repository/storage modules.

dependencies:
  - T01 DONE and integrated through workflow record 9cc7c62199a4b0ecb8d6ea51721200814e163035
  - shared ApiClient, ApiError, MSW handler composition, Vitest, and RTL foundation from T01

allowed_scope:
  - typed vehicle projection, vehicle view, list-query/list-response, filter-options, and minimal action-summary contract types required by the System Design
  - dealership-timezone-aware Aging Policy with injectable current instant/configuration
  - deterministic mock inventory fixtures including present and absent records plus deterministic lastSuccessfulSyncAt
  - mock inventory repository/read interface and inventory query/composition service
  - MSW handlers for GET /vehicles, GET /vehicles/:vehicleId, and GET /inventory/filter-options
  - typed frontend API functions for those three endpoint shapes
  - domain unit tests, HTTP/MSW contract tests, and narrow architecture tests for T02 requirements
  - minimal existing configuration changes strictly required to compile or run those tests

out_of_scope:
  - GET /inventory/summary, which is completed in T03
  - action statuses, stored action fixtures, action creation/history, persistence, eligibility, authorization, or mutation handlers
  - dashboard KPIs, inventory table/cards, filters UI, URL state, detail UI, or action forms
  - loading/error/stale UI, instrumentation, browser E2E, or broad design-system work
  - production Fastify, Prisma, PostgreSQL, synchronization worker, IdP, observability, or infrastructure
  - modifying System Design, decisions, Harness/core, plans, task contracts, or evidence
  - inventing production policy for sync intervals, maximum page size, unknown sort fields, or lifecycle-based action eligibility

acceptance:
  - VehicleProjection uses stable vehicleId for relations and keeps VIN as a business field; lifecycle and isPresentInLatestSnapshot are distinct.
  - inventoryAgeDays and isAging are absent from stored projection/fixture source data and derived only behind the HTTP boundary.
  - Aging Policy calculates dealership-local calendar-day difference and classifies only values greater than 90 as aging.
  - Literal unit examples prove 89 days is not aging, 90 days is not aging, and 91 days is aging.
  - At least one non-UTC boundary proves two instants that cross a UTC date but remain the same dealership-local date do not add an inventory day, or an equivalent independent worked example.
  - GET /vehicles excludes records not present in the latest snapshot while keeping upstreamStatus independent; a present SOLD or UNAVAILABLE fixture remains listable/filterable.
  - GET /vehicles applies make, model, ageMinDays, ageMaxDays, inventoryStatus, actionStatusId, and agingOnly behind HTTP before pagination.
  - In T02's empty-action transitional state, currentAction is null and actionStatusId returns no matches through the explicit action-read interface; T03 may replace that reader without moving filtering into React.
  - Default list sort is inventoryAgeDays descending; documented inventoryAgeDays ascending/descending sort is handled behind HTTP.
  - List metadata contains page, pageSize, total before pagination, and deterministic lastSuccessfulSyncAt; default page is 1 and default pageSize is 50.
  - GET /vehicles/:vehicleId returns a designed VehicleView for a present stable-ID fixture without frontend recomputation.
  - GET /inventory/filter-options returns sorted unique makes/models, and make narrows model options.
  - Typed frontend inventory API functions call the shared ApiClient; app/feature code does not import mock repository or storage.
  - No T03+ manager-action behavior, inventory summary, or UI is introduced.
  - Builder follows vertical red-to-green slices, commits coherent implementation, and returns exact evidence.

required_validation:
  - npm ci
  - npm run typecheck
  - npm run lint
  - npm run test
  - npm run build
  - Run or identify focused tests proving 89/90/91 and a non-UTC dealership-local boundary.
  - Run or identify HTTP tests proving filters happen before pagination, default oldest-first ordering, explicit ascending order, total/page metadata, detail by stable ID, make-specific models, current-presence semantics, lifecycle independence, and deterministic freshness.
  - Run or identify architecture proof that projection fixtures omit inventoryAgeDays/isAging and typed consumers cross ApiClient rather than mock repository/storage.
  - git diff --check 9cc7c62199a4b0ecb8d6ea51721200814e163035...<task_commit>
  - Verify the complete diff stays within T02 scope and exactly one package lockfile remains.

forbidden:
  - change System Design, accepted decisions, product semantics, or T01 contracts
  - persist inventoryAgeDays or isAging in projection fixtures
  - compute aging, filters, sorting, or pagination in React/app feature code
  - let frontend code import mock repository or browser storage
  - use VIN as relational identity
  - couple Inventory implementation directly to future action storage instead of an explicit read interface
  - implement action mutation/history/status/persistence or summary behavior early
  - add production backend or infrastructure
  - add arbitrary note, pagination-limit, lifecycle-eligibility, or synchronization policy
  - weaken tests or use implementation-coupled/tautological assertions
  - modify durable workflow artifacts from the Builder branch
  - perform unrelated cleanup

output:
  - coherent task commit or small coherent commit series based on 9cc7c62199a4b0ecb8d6ea51721200814e163035
  - concise evidence capsule using the repository template
  - requirement-to-implementation mapping
  - exact changed paths and command results
  - red-green evidence for each public seam
  - scope expansion and design deviations, which must be empty unless escalated
  - unresolved risks or blockers
---
