# T01 Evidence — Runnable Typed HTTP Frontend Foundation

## Provenance

- Task contract: `docs/agents/tasks/T01.md`
- Base commit: `c166671e61df0d36617090f226a4ffaf79960c23`
- Final reviewed implementation commit: `2b15a1519ddfe5ed805a97341099e617b22eca93`
- Reviewed tooling repair commit: `6ccd5a615643469c82e21ff2bd1faa69844f4b17`
- Integration commits on `main`: `55fe9416ebf8d4b24bab338f53400d2446aa9081`, `46a90b93c085050126b04bc48769d7fc2aa1cd4a`
- Final integrated tree commit before this evidence record: `46a90b93c085050126b04bc48769d7fc2aa1cd4a`

## Requirement Result

| Requirement | Result | Evidence |
|---|---|---|
| ARCH-HTTP-001 | PASS | Shared typed `ApiClient`; MSW interception test; architecture tests reject mock/storage access from app/API code. |
| ARCH-STATE-001 | PASS | Real Router + Query provider composition; test proves fetched state resides in `QueryClient` without another app-level store. |
| TECH-001 | PASS | React/TypeScript, TanStack Router/Query/Table, MSW, Vitest/RTL, and Playwright configuration are committed. |
| ERR-001 | PASS | `ApiError` preserves `code`, `message`, `requestId`, optional `details`; non-2xx test branches on stable code. |
| TEST-001 | PASS | Node/npm contract, one `package-lock.json`, and deterministic `typecheck`, `lint`, `test`, `build` scripts are committed. |

## Review And Repair

Initial fresh two-axis review found:

- `T01-SPEC-001` medium: invented production `GET /api/health`.
- `T01-SPEC-002` low: unsupported unused PUT/DELETE helpers.
- `T01-STD-001` medium: runtime/package-manager not pinned.
- `T01-STD-002` low: duplicated fallback error payload.
- `T01-STD-003` low: ambiguous `res` local name.

The Builder removed the production health endpoint, moved Query/MSW proof to test-only seams, removed PUT/DELETE helpers, pinned Node `24.18.1` and npm `11.16.0`, deduplicated fallback construction, and renamed the local. Re-review result:

- Standards: PASS
- Spec: PASS

Integration then exposed an untracked repository-local Herdr worktree. The Builder added only `.worktrees/` to `.gitignore`; targeted Standards and Spec re-review both passed, and integration restored clean coordination status.

## Independent Test Evidence

Fresh Codex Tester at `2b15a1519ddfe5ed805a97341099e617b22eca93`:

- `npm ci`: PASS; 266 packages, 0 vulnerabilities.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS.
- `npm run test`: PASS; 3 files, 7 tests.
- `npm run build`: PASS.
- `git diff --check c166671e61df0d36617090f226a4ffaf79960c23...HEAD`: PASS.
- Exactly one committed lockfile: PASS.
- Required named seams: MSW interception, stable `ApiError.code`, real Router + Query bootstrap, Query server-state ownership, and architecture import boundaries all PASS.

Fresh post-integration verification at `46a90b93c085050126b04bc48769d7fc2aa1cd4a`:

- `npm ci`: PASS; 266 packages, 0 vulnerabilities.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS.
- `npm run test`: PASS; 3 files, 7 tests.
- `npm run build`: PASS.
- `git status --short`: clean.
- `git check-ignore -v .worktrees/t01-foundation/package.json`: matched `.gitignore:21:.worktrees/`.

Vitest emitted jsdom's expected `Window.scrollTo() not implemented` diagnostic while all tests passed; this is non-blocking for T01 and is not counted as browser proof.

## Scope And Deviations

- Scope expansion: none.
- Design deviations: none.
- Production backend/infrastructure: not introduced.
- Inventory/action business behavior: not introduced.
- Remaining T01 blockers: none.

## Verdict

T01: DONE. T02 is READY.
