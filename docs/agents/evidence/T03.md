# T03 Evidence — Persistent Append-Only Manager Actions, Statuses, and Inventory Summary

## Provenance

- Task contract: `docs/agents/tasks/T03.md`
- Base commit: `129f6480503138aabcf9c8ab4bb348775ac86afb`
- Reviewed implementation branch: `task/t03-action-http`
- Final reviewed implementation commit: `3817c9cdf2f3bde2cd8b2abd1c71f7343bdc81f8`
- Integration commit on `main`: `ab8fb5ee5c5283d593f7d3b687a6957cff9cdb12`
- Final integrated tree commit before this evidence record: `ab8fb5ee5c5283d593f7d3b687a6957cff9cdb12`

## Requirement Result

| Requirement | Result | Evidence |
|---|---|---|
| INV-005 | PASS | `GET /inventory/summary` derives `totalInventory`, `agingVehicles`, `agingWithAction`, and deterministic `lastSuccessfulSyncAt` from present projections through the Aging Policy and current-action interface (`src/mocks/inventory/inventory-service.ts`; `src/api/inventory-composition.test.ts`; `src/mocks/inventory/inventory-service.test.ts`). |
| STAT-001 | PASS | `GET /vehicle-action-statuses` returns only active statuses in deterministic `sortOrder` order (`src/mocks/actions/status-catalog.ts`; `src/mocks/actions/status-catalog.test.ts`). |
| STAT-002 | PASS | Inactive status identities remain resolvable for historical actions and are rejected for new actions; identities are never deleted or repurposed (`status-catalog.test.ts`; `action-service.test.ts`). |
| ACT-001 | PASS | Actions are immutable/append-only, the newest persisted action is current, and no update/delete surface exists (no update/delete handlers in the diff; `action-service.test.ts`; `src/test/architecture.test.ts`). |
| ACT-002 | PASS | `GET /vehicles/:vehicleId/actions` returns complete immutable history newest-first with a deterministic append-order tie-breaker (`action-service.test.ts`; `src/api/actions.test.ts`). |
| ACT-003 | PASS | `POST` accepts only `statusId` plus an optional note; identity, timestamp, and trusted actor snapshot are generated behind HTTP; body-supplied `id`/`createdAt`/actor are rejected (`src/mocks/actions/action-service.ts`; `actions.test.ts`). |
| ACT-004 | PASS | Missing vehicle, absent-from-latest-snapshot, and non-aging targets are rejected with distinct codes `VEHICLE_NOT_FOUND`, `VEHICLE_NOT_PRESENT`, `VEHICLE_NOT_AGING`; lifecycle values do not add an eligibility gate (`actions.test.ts`; `action-service.test.ts`). |
| ACT-005 | PASS | Missing status and inactive status are rejected with `STATUS_NOT_FOUND` and `STATUS_INACTIVE` (`action-service.test.ts`; `actions.test.ts`). |
| ACT-006 | PASS | The default HTTP fixture injects an authorized actor that the POST body cannot override; an unauthorized trusted context is rejected with `UNAUTHORIZED`; the note is optional (omitted/null/empty/any string) (`action-service.ts`; `actions.test.ts`). |
| ACT-007 | PASS | Actions persist across a fresh mock module graph over the same browser storage and remain intact when inventory projections are replaced; browser storage is confined to the mock storage adapter (`storage-adapter.test.ts`; `actions.test.ts`; `architecture.test.ts`). |

## Review

Two fresh read-only DeepSeek Codex reviewer sessions reviewed `3817c9c` against `base_commit` `129f648`:

- Standards axis: PASS. All three prior findings closed with exact evidence — strict UTC-ISO instant/calendar round-trip validation with zero-write rejection (`storage-adapter.ts:29,72,155,167`), a per-import-declaration AST guard that rejects value imports of the authorized interfaces even after unrelated imports (`src/test/architecture.test.ts:39-92,349-366`), and a clean `git diff --check`.
- Spec axis: PASS. Every acceptance bullet and each of the eight pre-agreed test seams satisfied; no scope expansion.

Non-blocking observations (recorded, not defects):

- `SPEC-T03-01` (Info): the action service evaluates actor authorization before the vehicle/status gates, whereas System Design section 5.7 lists vehicle, presence, aging, status, then actor. Each single-condition rejection still returns its designated stable code, and no contract acceptance bullet requires precedence, so this is not a breach. If exact precedence is later required, reorder the authorization check after the vehicle/status gates or record precedence as a decision.
- `SPEC-T03-02` (Info): the storage adapter carries defensive hardening beyond the minimal persistence seam (strict validation, corrupt-JSON/shape rejection, throwing-getter guard, in-memory fallback). This stays inside the permitted adapter scope and introduces no product/UI/backend/auth/sync behavior.
- Standards note: the import guard inspects `ImportDeclaration` statements only; `export ... from` and dynamic `import()` forms are not flagged. No such construct exists in the diff; future hardening only.

## Independent Test Evidence

Fresh read-only DeepSeek Codex Tester at `3817c9c`:

- `npm ci`: PASS; 266 packages, 0 vulnerabilities.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS.
- `npm run test`: PASS; 11 files, 109 tests.
- `npm run build`: PASS.
- Focused T03 vitest (7 files): PASS; 88 tests.
- `git diff --check 129f648...3817c9c`: PASS.
- Exactly one committed lockfile; 23 changed paths all under `src/`.
- Named seams confirmed by test names: active-only listing plus inactive historical expansion; successful POST with server-generated identity; body non-override of actor/id/createdAt; omitted/null/empty-string note; immutable append; newest-first history with tie-breaker; current-action list/detail/`actionStatusId` composition; summary counts; all six rejection codes with `requestId`; AVAILABLE/RESERVED/SOLD/UNAVAILABLE lifecycle neutrality; persistence across a fresh graph and projection replacement; storage-adapter-only browser storage; ApiClient-only app access.

## Integration Verification

- DeepSeek Integrator applied the reviewed task via squash onto `main` as commit `ab8fb5ee5c5283d593f7d3b687a6957cff9cdb12`.
- `git diff ab8fb5e 3817c9c -- src` is empty (integrated src tree equals the reviewed task tree).
- `git diff --check 129f648..ab8fb5e`: clean; `git status --short`: clean.
- Post-integration verification on `main`: `npm ci` PASS (0 vulnerabilities), `npm run typecheck` PASS, `npm run lint` PASS, `npm run test` PASS (11 files, 109 tests), `npm run build` PASS.

## Scope And Deviations

- Scope expansion: none.
- Design deviations: none blocking. Two Info-level observations recorded above.
- Production backend/infrastructure, authentication, RBAC, synchronization, or UI: not introduced.
- Remaining T03 blockers: none.

## Verdict

T03: DONE. T04 is READY; T05 is serialized after T04 because they share the router/URL-shell seam.
