# T06 Evidence — Cross-Feature States, Business Errors, Freshness, and Instrumentation

## Provenance

- Task contract: `docs/agents/tasks/T06.md`
- Base commit: `1dd7cb6517c546dace4a625916135d4dd2401699`
- Reviewed implementation branch: `task/t06-integration`
- Final reviewed implementation commit: `7e77e773adf7c76801379f209f226ff0630e844e`
- Integration commit on `main`: `eb1056ec69b4632da78dae3487724e5dcaa21dd0`
- Final integrated tree commit before this evidence record: `eb1056ec69b4632da78dae3487724e5dcaa21dd0`

## Requirement Result

| Requirement | Result | Evidence |
|---|---|---|
| ERR-002 | PASS | Feature code classifies and branches on the stable `ApiError.code` only (`src/features/errors/business-errors.ts`), with no message parsing; an expected `VEHICLE_NOT_AGING` rejection is contained to the action-form region with zero client-error telemetry and the other regions intact (`src/features/actions/action-outcomes.test.tsx`). |
| OBS-001 | PASS | `src/features/observability/instrumentation.ts` is an injectable-sink boundary for client errors and Web Vitals; report shapes copy only type/code/requestId/status/source/endpoint and structurally drop message, details, and free-text notes. Tests assert a distinctive note value never reaches the sink after success or rollback (`instrumentation.test.ts`, `action-outcomes.test.tsx`). |
| ACT-010 | PASS | After a successful create and after a rollback, list rows, detail current action, detail history, and inventory summary stay coherent through TanStack Query invalidation/refetch (`src/features/actions/queries.ts`; T06 coherence tests). |
| UI-STATE-001 | PASS | Loading renders localized KPI, list, and detail skeletons while the application shell stays present (`src/features/inventory/components/loading-skeleton.tsx`; T06 shell-preservation test). |
| UI-STATE-003 | PASS | A list or detail API failure is contained to that region with a labelled retry, leaving the other regions usable (`components/regional-error.tsx`; T06 regional tests for list and detail). |
| UI-STATE-004 | PASS | Sync lag is computed from `lastSuccessfulSyncAt` and a controllable current time; above the configured freshness threshold (demo default 30 minutes) the dashboard retains the previous inventory, shows a `Last updated ...` label, and warns that inventory may be outdated, and below the threshold no warning is shown (`src/features/inventory/freshness.ts`, `components/freshness-notice.tsx`; boundary unit + integration tests). |
| TEST-003 | PASS | All cross-feature behaviors are proven through RTL + MSW integration tests driven through the real App and MSW handlers. |

## Review

Fresh read-only DeepSeek Codex reviewer sessions reviewed `7e77e77` against `base_commit` `1dd7cb6`:

- Standards axis: PASS. All eight checked criteria confirmed; no boundary violation.
- Spec axis: PASS. Every acceptance bullet and declared seam traced to evidence.

Recorded non-blocking findings and their disposition:

- `T06-STD-F1` (low, test maintainability): the new integration tests duplicate helpers (`renderInventory` twice, `createRecordingSink` twice, threshold constants twice) instead of sharing them via `src/test`. Disposition: accepted low; no repair required for release, recorded for a future cleanup.
- `T06-STD-F2` (low, instrumentation composition): the running app installs the module-level no-op instrumentation for global error/Web Vitals reporting, while the injectable sink is reachable only through the App instrumentation context, so an injected test sink receives mutation errors but not global errors or Web Vitals. Impact is low because the production telemetry backend is design-only scope and the note-exclusion guarantee holds. Disposition: accepted low, recorded; the boundary required by OBS-001 exists and excludes notes.
- `T06-SPEC-OBS` (info): the results region is gated on `listQuery.isSuccess`, so a failed background refetch could replace cached rows with the regional error. The design retention language (6.9/8.5) is scoped to staleness and the error path is correctly contained with retry, so this is robustness polish rather than a T06 spec gap. Disposition: accepted.

## Independent Test Evidence

Fresh read-only DeepSeek Codex Tester at `7e77e77`:

- `npm ci`, `npm run typecheck`, `npm run lint`, `npm run build`: all PASS.
- `npm run test`: PASS; 20 files / 172 tests, green across 3 consecutive full-suite runs.
- Focused T06 vitest (5 files): PASS; 27 tests.
- `git diff --check 1dd7cb6...7e77e77`: clean; exactly one committed package lockfile; 20 changed paths all under `src/`.
- Named seams confirmed by passing test names: `ApiError.code` branching with no message parsing; regional business rejection containment; regional list and detail error + retry; localized skeletons with the shell present; freshness warning above/below threshold with retained inventory; cross-feature coherence after success and after rollback; instrumentation boundary never reporting a free-text note.
- Adversarial probes on a scratch `/tmp` copy (repo untouched): making telemetry unconditional for expected rejections, leaking the note into the reported payload, and replacing the shell with a global spinner each failed exactly the matching named test, confirming the assertions are load-bearing.

## Integration Verification

- DeepSeek Integrator applied the reviewed work via squash onto `main` as commit `eb1056ec69b4632da78dae3487724e5dcaa21dd0`.
- `git diff --name-only eb1056e 7e77e77` lists only `docs/agents/tasks/T06.md`.
- `git diff --check 1dd7cb6..eb1056e`: clean; `git status --short`: clean.
- Post-integration verification on `main`: `npm ci` PASS, `npm run typecheck` PASS, `npm run lint` PASS, `npm run test` PASS (20 files, 172 tests), `npm run build` PASS.

## Scope And Deviations

- Scope expansion: none.
- Design deviations: none blocking. Two low findings and one info observation recorded with dispositions.
- No mock/API semantic change; no production/infrastructure code; no T07/T08 behavior; no dependency or lockfile change.
- Remaining T06 blockers: none.

## Verdict

T06: DONE. T07 and T08 are READY for the parallel verification wave.
