# T02 Evidence — Authoritative Mock Inventory Reads and Aging

## Provenance

- Task contract: `docs/agents/tasks/T02.md`
- Base commit: `9cc7c62199a4b0ecb8d6ea51721200814e163035`
- Final reviewed implementation commit: `30891cdb5307cb5e72920ffbbe09e0d8914514b7`
- Integration commit on `main`: `92bdc3f26ce7e88261a432279bdbbdae6a009a53`
- Final integrated tree commit before this evidence record: `92bdc3f26ce7e88261a432279bdbbdae6a009a53`

## Requirement Result

| Requirement | Result | Evidence |
|---|---|---|
| ARCH-DOM-001 | PASS | `VehicleProjection` uses stable `vehicleId` for relations; VIN is a business field; lifecycle and `isPresentInLatestSnapshot` are distinct. |
| AGE-001 | PASS | Dealership-local calendar aging policy calculates exact day difference; aging is strictly `inventoryAgeDays > 90`. |
| AGE-002 | PASS | Aging is derived behind HTTP/MSW boundary; fixtures and app code do not persist or recompute `inventoryAgeDays` or `isAging`. |
| INV-001 | PASS | Documented list filters (`make`, `model`, `ageMinDays`, `ageMaxDays`, `inventoryStatus`, `actionStatusId`, `agingOnly`) apply behind HTTP before pagination. |
| INV-002 | PASS | Server-side sorting behind HTTP defaults to `inventoryAgeDays DESC`; handles documented ascending/descending order. |
| INV-003 | PASS | Page-based `VehicleListResponse` returns page, pageSize, total before pagination, and deterministic `lastSuccessfulSyncAt`. |
| INV-004 | PASS | `GET /vehicles/:vehicleId` retrieves vehicle view by stable ID without frontend recomputation. |
| INV-006 | PASS | `GET /inventory/filter-options` returns dynamic unique sorted makes and models; make narrows model options. |
| INV-007 | PASS | Deterministic fixtures distinguish lifecycle from snapshot presence and expose deterministic freshness timestamp. |
| TEST-002 | PASS | Focused tests prove 89/90/91 boundary thresholds and non-UTC dealership-local calendar day boundary. |

## Review And Repair

Initial fresh review findings were repaired before the final commit:

- Strict sort union instead of arbitrary-string fallback.
- Removed speculative positional aging overload.
- Introduced and injected `VehicleProjectionReader`.
- Removed an unused action-reader method.
- Added HTTP/MSW proof that model and age range traverse the request boundary before pagination.

Fresh final Codex reviews at final task commit `30891cdb5307cb5e72920ffbbe09e0d8914514b7`:

- Standards: PASS
- Spec: PASS

## Independent Test Evidence

Fresh Codex Tester at `30891cdb5307cb5e72920ffbbe09e0d8914514b7`:

- `npm ci`: PASS; 0 vulnerabilities.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS.
- `npm run test`: PASS; 6 files, 41 tests.
- `npm run build`: PASS.
- Verified 89/90/91 thresholds, non-UTC behavior, derived aging, filters before pagination, lifecycle/presence, detail, filter options, mock-only architecture boundaries, and no T03/UI scope.

## Integration Verification

- AGY Integrator integrated the task as commit `92bdc3f26ce7e88261a432279bdbbdae6a009a53`.
- Codex verified task-tree equality for all changed paths.
- Fresh post-integration verification on `main`:
  - `npm ci`: PASS; 0 vulnerabilities.
  - `npm run typecheck`: PASS.
  - `npm run lint`: PASS.
  - `npm run test`: PASS; 6 files, 41 tests.
  - `npm run build`: PASS.
  - `git status --short`: clean (main worktree clean).

## Scope And Deviations

- Scope expansion: none.
- Design deviations: none.
- Production backend/infrastructure: not introduced.
- T03 manager-action persistence/mutation/history/statuses or UI: not introduced.
- Remaining T02 blockers: none.

## Verdict

T02: DONE. T03 is READY.
