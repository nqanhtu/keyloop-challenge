# U06 — Bounded Repairs (evidence)

## Repair scope (Codex Lead, reconciled from the U04 and U05 finding sets)

Finding mapping: RP-1 = U04 F-04 + U05 F1 + U05 F5 (modal focus containment and inert background); RP-2 = U05 F2 (muted text token contrast); RP-3 = U04 F-03 + U05 F3 (Aging only touch target); RP-4 = U04 F-01 (desktop first-viewport density); RP-5 = U04 F-02 (tablet compact row density); RP-6 = U04 F-05 (tablet filter-sheet geometry); RP-7 = U05 F4 (main landmark); RP-8 = U04 F-06 (duplicate AGING); RP-9 = U04 F-07 (no-action scannability); RP-10 = U04 F-08 (ineffective sticky header); RP-11 = U04 F-09 (KPI card overflow); RP-12 = R-01 (modal background scroll lock, the residual of U05 F1).

Lead rulings applied: no skip link (the preserved contract fixes the first two tab stops); the dense desktop table keeps the UI section 8 exemption; Clear all keeps sort; KPI values stay collection-wide.

## U06 — task/u06-repairs

- base_commit: 989e7c8
- commits: 66d58e9 fix(tokens): darken muted text token to WCAG AA (RP-2); 26caadf fix(ui): repair U06 findings - modal focus, density, sheet, aging/action, landmark (RP-1, RP-3..RP-11)
- integration: merge 1f9b73e "merge: integrate U06 UI repairs" (no conflicts, commits preserved unsquashed, no content altered, nothing pushed)
- changed paths: e2e/accessibility.spec.ts; src/app/styles/primitives.css; src/app/styles/styles.test.ts; src/app/styles/tokens.css; src/app/ui/focus-trap.ts (new); src/app/ui/index.ts; src/features/actions/vehicle-detail.tsx; src/features/inventory/inventory-dashboard.tsx; src/features/inventory/inventory-dashboard.css; src/features/inventory/inventory-dashboard.test.tsx; src/features/inventory/components/filter-panel.tsx; src/features/inventory/components/vehicle-cards.tsx; src/features/inventory/components/vehicle-table.tsx
- Codex Lead diff inspection: the two touched test files gained additive tests only (modal focus containment in e2e/accessibility.spec.ts; muted-token contrast in src/app/styles/styles.test.ts; no-action pill in src/features/inventory/inventory-dashboard.test.tsx); no existing assertion was modified.

## U06b — task/u06b-scrolllock

- base_commit: 1f9b73e
- commit: dbb908c fix(ui): lock background page scroll while a modal is open (R-01)
- integration: merge 93a62fd "merge: integrate U06b modal scroll lock" (no conflicts, nothing pushed)
- changed paths: e2e/accessibility.spec.ts (one additive scroll-lock test); src/features/inventory/inventory-dashboard.tsx (document-scroll lock while any modal surface is open, restored exactly on close)
- Lead diff inspection: additive test only; no preserved selector, accessible name, id, data attribute or column header renamed.

## Repairer-reported before/after measurements

- RP-1: focus escaped to body and background pagination/rows at all tiers -> 25 Tab and 12 Shift+Tab presses at each tier stay inside the dialog, background subtree inert, zero landings on body.
- RP-2: muted token #6b7a90 measured 4.36:1 on white and 3.88:1 on the table-header surface -> #5b6b80 measures 5.44:1 on white, 4.84:1 on the muted surface, 5.16:1 on canvas.
- RP-3: Aging only hit area 108x28 at tablet and mobile -> 108x44.
- RP-4: desktop discovery panel 272 px -> 198 px; filter band one line using about 91 percent of its width; fully visible rows 1 -> 2, with a bounded results region showing 5 rows at once.
- RP-5: tablet row 97 px vs desktop 81 px -> tablet 69 px vs desktop 84 px, status and Details on one line, Details target 61.81x44 with an unchanged accessible name.
- RP-6: tablet sheet groups a fixed 168 px box holding 65 px of content -> content-sized 90.78 px groups with all fields 387 px in a shared grid.
- RP-7: main landmark count 0 -> 1 on /inventory; no skip link added; first two tab stops still #inventory-sort then #inventory-aging-only.
- RP-8: AGING printed twice per region -> exactly one readable AGING statement per mobile card and in the detail summary.
- RP-9: no-action and has-action rows typographically identical -> no-action is a dashed neutral pill, has-action a solid neutral pill (border, fill and wording, not colour alone).
- RP-10: sticky header ineffective (header top -763 px after scrolling 1400) -> results region bounded (max-height min(68vh, 44rem)) with the header staying inside the scrollport; no horizontal overflow reintroduced.
- RP-11: 21 px page overflow at the 195 px probe from fixed 200 px cards -> fluid cards, 0 px overflow at 195/320/360/390/1280.
- RP-12 (R-01): background page scrolled 0 -> 410 while the detail was open -> page scroll held while any modal surface is open at all three tiers, modal content still scrolls, normal scrolling and the bounded results region work after close.

## Fresh independent re-verification (u06-verify, read-only, candidate 1f9b73e)

- All of RP-1 .. RP-11 verified FIXED with fresh Chromium measurements; the Preserved Observable Contracts re-derived in the live DOM held (40/40 checks) and no regression was introduced.
- Residual reported at that time: R-01 (background scroll) and a desktop density caveat; R-01 was then repaired by U06b.

## Accepted residual (Codex Lead)

- At 1280x800 the desktop first viewport shows two fully visible result rows, and the results region is a bounded scroll region showing five rows simultaneously. The filter band is now a single line using about 91 percent of its width and the results region is the largest first-viewport region. Codex Lead accepts this as consistent with UI System Design sections 4.5, 6.1, 12.1 and 22; there is no MUST-level overflow, density or hierarchy violation.
